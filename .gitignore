node_modules/
frontend/dist/
__pycache__/
*.pyc
.env
.DS_Store
*.egg-info/

# RoomArt gerçek satış/ciro agregatı — PUBLIC repo'ya commit'lenmemeli (rakip istihbaratı).
# Sadece yerel + Firestore'a (private) yazılmalı. (Her iki yol da: data + frontend kopyası.)
data/trendyol_sales.json
frontend/public/data/trendyol_sales.json

# Yerel inceleme dökümleri (hassas: net satış) — commit'lenmez
reports/
# Yerel platform creds (ASLA commit'lenmez)
backend/.env.trendyol.local
.env.trendyol.local
backend/.env.hb.local
backend/.env.n11.local
backend/.env.shopify.local
backend/.env.judgeme.local

# Platform satış verileri (gelir = rekabetçi istihbarat — private kalır)
data/hb_sales.json
data/n11_sales.json
data/shopify_sales.json
frontend/public/data/hb_sales.json
frontend/public/data/n11_sales.json
frontend/public/data/shopify_sales.json

# CLEAR karar katmanı — MANUEL maliyet/marj girdisi (birim maliyet = en hassas veri).
# Ham CSV'ler ve türetilmiş marj içeren çıktı ASLA public repo'ya gitmez;
# dashboard'a yalnız Firestore (private, authed) üzerinden servis edilir. (trendyol_sales.json deseni.)
data/manual_margin_inputs.csv
data/manual_operation_inputs.csv
data/clear_scores.json
frontend/public/data/clear_scores.json

# Çok-platform ürün kütüğü — platform ciro/satış agregatı içerir (rakip istihbaratı).
# Yalnız Firestore (private) + yerel; public repo'ya gitmez.
data/product_registry.json
frontend/public/data/product_registry.json

# Platform yorum/yıldız scrape verisi
data/platform_reviews.json
